# CIPH v0.24 Experience Diagnostics Implementation Plan

## Scope

1. Implement deterministic diagnostics engine in `scripts/diagnose_experience_store.py`.
2. Validate findings coverage with focused unit tests.
3. Sync docs (`README.md`, `harness/CIPH.md`) and Proofline assets (`skills/proofline/*`).
4. Update Proofline install/installation tests for new command availability and smoke execution.
5. Run targeted and end-to-end checks: unit tests, manifest checks, and diagnostics reports.

## Implementation Steps

1. Add record extraction from manifests, traces, evaluations, and replay receipts.
2. Add finding rules for contract and readiness risks.
3. Add recommendation rollup with stable sorting and dedupe.
4. Add CLI (`--min-severity`, `--format`, `--output`) and report rendering.
5. Add run-level diagnostics artifacts under a new v0.24 run (optional) to make the layer visible in history.
